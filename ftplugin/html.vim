" ============================================================================
" HTML-specific Configuration
" ============================================================================

" Disable ALE auto-fix on save for HTML files
" This prevents prettier from auto-formatting HTML on save
let b:ale_fix_on_save = 0

" You can still manually format with :ALEFix when needed
